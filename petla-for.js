// wygląd pętli
// for(wartosc od jakiej ma liczyc; do jakiej wartosci; dekrementacja lub inkrementacja petli)

let licznik = 0;

for (licznik; licznik <= 5; licznik++) {
  console.log("test" + licznik);
}
